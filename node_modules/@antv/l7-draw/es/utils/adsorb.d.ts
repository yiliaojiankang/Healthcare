import { AdsorbResult, IAdsorbOptions } from '../typings';
import { LineMode, PolygonMode } from '../mode';
import { Feature, LineString, Point, Position } from '@turf/turf';
import { Scene } from '@antv/l7';
/**
 * 获取当前数据对应的吸附点、线数组
 * @param adsorbDataConfig
 * @param draw
 * @param position
 */
export declare const getAdsorbFeature: (adsorbDataConfig: IAdsorbOptions['data'], draw: LineMode<any> | PolygonMode<any>, position: Position) => AdsorbResult;
export declare const getAdsorbPoint: (position: Position, points: Feature<Point>[], options: IAdsorbOptions, scene: Scene) => Position | null;
export declare const getAdsorbLine: (position: Position, lines: Feature<LineString>[], options: IAdsorbOptions, scene: Scene) => Position | null;
