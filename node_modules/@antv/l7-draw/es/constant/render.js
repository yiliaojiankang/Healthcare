import { DashLineRender, LineRender, MidPointRender, PointRender, PolygonRender, TextRender } from '../render';
/**
 * renderType与render的映射
 */
export var RENDER_MAP = {
  point: PointRender,
  line: LineRender,
  dashLine: DashLineRender,
  midPoint: MidPointRender,
  text: TextRender,
  polygon: PolygonRender
};