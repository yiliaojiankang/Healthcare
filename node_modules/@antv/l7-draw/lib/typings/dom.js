"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getParentByClassName = void 0;
/**
 * 遍历当前结点 dom 的本身及其所有父节点，直到找到对应
 * @param dom
 * @param className
 * @returns
 */
var getParentByClassName = exports.getParentByClassName = function getParentByClassName(dom, className) {
  var currentDom = dom;
  while (currentDom && !Array.from(currentDom.classList).includes(className)) {
    currentDom = currentDom.parentElement;
  }
  return currentDom;
};